country_decisions = {
	miremen_nation = {
		major = yes
		potential = {
			NOT = { has_country_flag = formed_ayaloquur_flag }
			NOT = { tag = A38 } #Ayalôquur
            culture_group = miremen_group
            OR = {
                tag = A26 #Lô'myyr
                tag = A27 #Lô'tyyr
                tag = A23 #Sveppareyja
            }
			is_colonial_nation = no
			OR = {
				ai = no
				is_playing_custom_nation = no
			}
			OR = {
				is_former_colonial_nation = no
				AND = {
					is_former_colonial_nation = yes
					ai = no
				}
			}
			normal_or_historical_nations = yes
			#was_never_end_game_tag_trigger = yes
		}
		provinces_to_highlight = {
            OR = {
                province_group = myrish_swamps
                province_group = tyrian_swamps
            }
			NOT = { country_or_non_sovereign_subject_holds = ROOT }
			NOT = {
				ROOT = { 
					num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
						value = 18
						OR = {
                            province_group = myrish_swamps
                            province_group = tyrian_swamps
                        }
					}
				}
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_nomad = no
			is_at_war = no
			NOT = { exists = A38 } #Ayalôquur
            num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				value = 18
				OR = {
                    province_group = myrish_swamps
                    province_group = tyrian_swamps
                }
			}
		}
		effect = {
			hidden_effect = { restore_country_name = yes }
			change_tag = A38
			swap_non_generic_missions = yes
			remove_non_electors_emperors_from_empire_effect = yes
			if = {
				limit = {
					NOT = { government_rank = 3 }
				}
				set_government_rank = 3
			}
			aldovir_superregion = {
				limit = {
					NOT = { owned_by = ROOT }
				}
				add_permanent_claim = A38
			}
			add_prestige = 25
			add_country_modifier = {
				name = "centralization_modifier"
				duration = 7300
			}
			set_country_flag = formed_ayaloquur_flag
			if = {
				limit = { 
					has_custom_ideas = no
				}
				country_event = { id = ideagroups.1 } #Swap Ideas
			}
		}
		ai_will_do = {
			factor = 1
		}
		#ai_importance = 400
	}
	
}-